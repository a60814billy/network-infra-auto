---
inventory:
  plugin: SimpleInventory
  options:
    host_file: test_inventory/hosts.yaml
    group_file: test_inventory/groups.yaml
    defaults_file: test_inventory/defaults.yaml

runner:
  plugin: threaded
  options:
    num_workers: 10
